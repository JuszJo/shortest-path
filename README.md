# shortest-path

Finds the shortest path between 2 nodes using vanilla **Javascript**.

This program utilizses the **Breadth First Search Algorithm** to find the node we're searching for
then searches for the shortest path/paths between the starting node and the searched node
